# Nautilus Portal — зависимости
# Ядро портала использует только стандартную библиотеку Python 3.10+.
# Нет обязательных внешних пакетов.

# Опционально: для glyph_adapter.py (ML-компонент, не часть оркестратора)
# torch>=2.0
